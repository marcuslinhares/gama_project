# E2E Testing Implementation Plan (Playwright)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set up and implement automated end-to-end tests using Playwright for the critical purchase flow.

**Architecture:** Playwright tests running against the dev server. Magic OTP bypass for test user authentication.

**Tech Stack:** Playwright, Node.js, TypeScript.

---

### Task 1: Magic OTP for Testing

**Files:**
- Modify: `backend/src/controllers/auth.controller.ts`
- Modify: `backend/src/db/migrations/003_create_users.sql` (re-apply or manual insert)

- [ ] **Step 1: Create test user in DB**

```sql
INSERT INTO users (phone, name, distributor_id, role) 
VALUES ('5588000000000', 'Robô de Teste', '00000000-0000-0000-0000-000000000000', 'MERCHANT')
ON CONFLICT (phone) DO NOTHING;
```

- [ ] **Step 2: Add Magic OTP bypass in `verifyOTP`**

```typescript
// backend/src/controllers/auth.controller.ts
if (phone === '5588000000000' && code === '999999') {
    // Return early with a valid JWT for the test user
}
```

- [ ] **Step 3: Commit**

---

### Task 2: Playwright Setup

**Files:**
- Create: `playwright.config.ts`
- Modify: `frontend/package.json`

- [ ] **Step 1: Install Playwright dependencies in the frontend**
- [ ] **Step 2: Create basic config pointing to http://localhost:5173**
- [ ] **Step 3: Commit**

---

### Task 3: Implementation of Checkout Test

**Files:**
- Create: `frontend/tests/checkout.spec.ts`

- [ ] **Step 1: Write test for Login -> Add to Cart -> Checkout -> Success**
- [ ] **Step 2: Implement selectors using Playwright locators (e.g., getByText, getByPlaceholder)**
- [ ] **Step 3: Run and verify the test passes**
- [ ] **Step 4: Commit**
